{
  "name": "bigbrother-barber-bot",
  "version": "1.0.0",
  "description": "WhatsApp chatbot for Big Brother Barber Shop using Meta Cloud API + AWS Lambda",
  "private": true,
  "scripts": {
    "test": "node tests/chatbot.test.js",
    "local": "node scripts/localTest.js"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.600.0",
    "@aws-sdk/lib-dynamodb": "^3.600.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20.0.0"
  }
}
